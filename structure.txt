driveSense-chatbot/
├─ package.json                 # Root package.json with concurrently + "dev" script
├─ node_modules/                # Root dependencies (concurrently)
│
├─ server/
│  ├─ package.json              # Server dependencies (express, mongoose, etc.)
│  ├─ .env                      # Environment variables (MONGO_URI, OPENAI_API_KEY, PORT)
│  ├─ index.js                  # Express app setup with CORS and route imports
│  │
│  ├─ config/
│  │  ├─ db.js                  # MongoDB connection using mongoose
│  │  └─ openai.js              # OpenAI client configuration
│  │
│  ├─ models/
│  │  ├─ Vehicle.js             # Vehicle schema
│  │  └─ Message.js             # Chat history schema
│  │
│  ├─ routes/
│  │  ├─ chat.js                # Chat route with rule-based response system
│  │  ├─ vehicleRoutes.js       # Vehicle routes (GET, POST /seed, GET /debug, GET /search)
│  │  └─ history.js             # Chat history routes
│  │
│  └─ scripts/
│     └─ seedVehicles.js        # Database seeding script
│
└─ client/
   ├─ package.json              # Vite + React configuration
   ├─ .env                      # Client environment variables (VITE_API_URL)
   ├─ index.html                # HTML entry point
   ├─ vite.config.js            # Vite configuration
   │
   └─ src/
      ├─ main.jsx               # React app entry point
      ├─ App.jsx                # Main app layout with Router, ThemeProvider, AnimationProvider
      ├─ api.js                 # Axios instance for API calls
      ├─ index.css              # Global styles, Tailwind imports, CSS variables, base styles
      │
      ├─ components/            # Reusable UI components
      │  ├─ Navbar.jsx          # Top navigation with animation dropdown (Updated)
      │  ├─ Footer.jsx          # Simple footer component
      │  ├─ ScrollToTop.jsx     # Button to scroll to the top of the page
      │  ├─ VehicleInfo.jsx     # Component to display structured vehicle data
      │  │
      │  └─ BackgroundAnimation/
      │     ├─ AnimationSelector.jsx   # Main selector for animation components (New)
      │     ├─ AnimatedBackground.jsx # Wrapper component (Updated)
      │     ├─ HighwayAnimation.jsx    # Highway night scene animation (New)
      │     ├─ DashboardAnimation.jsx  # Car dashboard animation (New)
      │     ├─ EngineAnimation.jsx     # Engine parts animation (New)
      │     ├─ CityscapeAnimation.jsx  # City scene with cars (New)
      │     ├─ BlueprintAnimation.jsx  # Technical blueprint animation (New)
      │     └─ background.css          # CSS for legacy animations (Kept for reference)
      │
      ├─ context/               # React Context providers
      │  ├─ ThemeContext.jsx    # Context for managing and toggling dark/light theme
      │  └─ AnimationContext.jsx # Context for managing selected animation (New)
      │
      ├─ pages/                 # Page-level components
      │  └─ ChatHome.jsx        # Main chat interface
      │
      ├─ styles/                # Component-specific CSS
      │  ├─ chatHome.css        # Styles for the chat interface
      │  └─ Navbar.css          # Styles for the Navbar component (Updated)
      │
      └─ assets/                # Default Vite assets
         ├─ react.svg
         └─ vite.svg